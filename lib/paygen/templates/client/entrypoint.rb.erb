# frozen_string_literal: true

# Сгенерировано paygen <%= paygen_version %> по спецификации <%= api.title %> v<%= api.version %>.
# Файл перегенерируется — правьте спеку или шаблоны, а не этот код.

require_relative "<%= gem_name %>/version"
require_relative "<%= gem_name %>/errors"
require_relative "<%= gem_name %>/client"
<% api.resources.each do |resource| -%>
require_relative "<%= gem_name %>/resources/<%= resource.file_name %>"
<% end -%>

module <%= module_name %>
  def self.new(**options) = Client.new(**options)
end
