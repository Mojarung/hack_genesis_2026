# План на хакатон (3–6 сентября 2026)

## Где мы сейчас (ночь 3 → 4 сентября)

Готово и проверено:

- конвейер роутинга: 13 hard-правил (включая валюту и предохранитель), 12 целей скоринга (включая bank affinity
  и ожидаемую маржу), каскад попыток с fallback, честная модель времени, два режима симуляции;
- `routing_decisions.json` с полным трейсом и разложением скора, `routing_report.json` с распределением,
  загрузкой лимитов, достижимостью целей, историей и 12 правилами рекомендаций, `routing_report.html`;
- что-если: `backtest` (leave-one-out), `compare`, `simulate` (Monte-Carlo), `tune` (подбор весов);
- HTTP-сервис `serve` с метриками Prometheus; Docker; CI;
- 181 спек (покрытие 97.8%), rubocop чист, бенчмарк ≈3 400 заявок/с;
- скрипт автопроверки организаторов: 29 проверок, 0 ошибок; эталонные кейсы совпадают.

## За счёт чего побеждаем

Баллы экспертов (100) и жюри (140 + 20) разложены по критериям — под каждый есть артефакт, который
показывается за 30 секунд. Сценарий защиты — `docs/pitch.md`.

| Критерий | Баллы | Чем закрываем | Что показать |
|---|---|---|---|
| Hard + soft при выборе, fallback | 22 + 22 | слои `Constraints`/`Scoring`, `Router#route` с повтором и fallback, `State` по времени | `explain op_105 --simulation conversion --seed 7`: отказ vipay → quickpay |
| Гибкость: 7 стратегий, совместный учёт, конфиг | 33 + 32 | все стратегии ТЗ — цели с весами, каскад — частный случай, пять пресетов | `compare`: одна очередь, пять политик, разное распределение |
| Согласование целей | 15 | взвешенный скор + tie-breakers + `target_attainability` + `share_shortfall` | `breakdown`: `traffic_share` против `load` у payflow (99.6% лимита) |
| Объяснимость | 15 + 10 | `reason`/`details` на каждой попытке, `violations`, `lower_score`, `--why-not` | `routing_decisions.json`, `explain` |
| Качество реализации | 20 + 10 | слои, реестры, `Data`, валидация входов, коды выхода, 98% покрытия, CI | `bundle exec rake`, `docs/architecture.md` |
| Аналитика и выводы | 10 + 11 | `distribution` с отклонением, `results`, `projected_daily_utilization`, `history_analysis`, рекомендации с параметром | `routing_report.html`, `backtest`: +6.9% одобрений |
| Наличие файлов | 40 | `rake submit` + валидатор до коммита | файлы в корне `main` |
| Доп. идеи, полнота, выступление | 20 | bank affinity, backtest, Monte-Carlo, tune, serve, предохранитель | демо по питчу |

Что отличает от «типового» решения (у других команд, работающих с тем же ассистентом, будет базовый
скоринг и отчёт): (1) модель времени вместо мгновенных счётчиков; (2) bank affinity и бэктест на истории
организаторов с честной leave-one-out оценкой и цифрой «+6.9%»; (3) что-если: сравнение политик, Monte-Carlo,
подбор весов; (4) прод-контур: HTTP-сервис, метрики, предохранитель, Docker, CI; (5) рекомендации с параметром
и значением; (6) один механизм скоринга для всех стратегий, каскад — просто веса.

## 4 сентября — чекпоинт 1: «архитектура и корректность»

1. Прогнать питч вслух по `docs/pitch.md` с таймером; открыть `routing_report.html` на проекторе.
2. Пройтись по ТЗ построчно: каждая фраза критериев — в тест или пример (`spec/integration`).
3. Добить покрытие ветвей до 85%; стресс-сценарии: провайдер выключается посреди очереди, все лимиты
   исчерпаны, пустая очередь, заявка без банка/времени, 100 000 заявок.
4. Спросить экспертов, какие поля будут в `operations_queue_test.json` и `providers` для теста.

## 5 сентября — чекпоинт 2: «гибкость и аналитика»

1. `calibrate`: предложить `volume_share_pct`, `requests_per_minute_limit` (пиковая интенсивность по истории)
   и наблюдаемую конверсию как патч к политике.
2. Дашборд: вкладка сравнения политик и Monte-Carlo прямо в HTML.
3. Сценарий «провайдер лёг»: демо предохранителя и рекомендаций в режиме `conversion`.
4. Слайды: 7–8 штук по питчу, живое демо в терминале.

## 6 сентября — чекпоинт 3 и стопкод

1. За час до стопкода: `operations_queue_test.json` → `data/`, `bundle exec rake submit`, проверить вывод
   валидатора, закоммитить `routing_decisions_test.json` и `routing_report_test.json` в корень `main`
   (ветку слить заранее, чтобы в последний час был один коммит).
2. Заморозить политику по умолчанию: режим `optimistic` (детерминирован, эталоны проходят);
   `conversion` — только для демо.
3. Финальный прогон `bundle exec rake` на чистом клоне (или в Docker).

## Риски и страховки

| Риск | Страховка |
|---|---|
| Тестовая очередь с неожиданной структурой (нет `created_at`, `bank` в другом регистре, дубликаты) | загрузчик подставляет время, нормализует банк, дубликаты — явная ошибка с id; прогнать `validate` до коммита |
| Эталонные кейсы жюри требуют «единственного допустимого» | режим `optimistic` не меняет выбор; hard-правила — надмножество валидатора организаторов |
| Провайдер в тестовом снимке без лимитов / с новыми полями | `null` = «лимита нет», неизвестные поля игнорируются, известные проверяются по типу |
| История для теста может отсутствовать | без истории `bank_affinity` нейтральна, остальное работает; `--history ""` |
| Файлы не в корне / не в `main` | `rake submit` пишет в корень; чек-лист в README |
